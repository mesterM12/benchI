# Domain docs

This repository uses a single-context domain layout.

Before domain-sensitive work, read `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist. Proceed silently when they do not.

Use glossary terms from `CONTEXT.md` consistently. If work conflicts with an ADR, surface the conflict rather than silently overriding it.
